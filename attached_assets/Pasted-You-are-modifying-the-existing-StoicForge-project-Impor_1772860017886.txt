You are modifying the existing StoicForge project.

Important constraints:

* Do NOT change the existing UI layout or design.
* Do NOT remove or rewrite working systems.
* Only improve logic and small UI behaviors.
* The application runs fully on the frontend and stores data using localStorage.

Implement the following improvements.

---

1. Fix the focus timer so it continues when the user changes browser tabs.

Current issue:
The timer pauses when the StoicForge tab is not active.

Required behavior:

* When the user presses Start, store a focus start timestamp.
* The timer display must calculate elapsed time using the stored timestamp instead of relying only on setInterval.
* The timer must continue correctly if the user switches tabs, minimizes the browser, refreshes the page, or returns later.
* If the page loads and a focus session was previously running, restore the running session automatically.

Do not modify the existing XP reward system.

---

2. Handle focus sessions that cross midnight.

Example:
Session starts at 11:00 PM and ends at 2:00 AM.

Required behavior:

* The focus session remains one continuous session.
* However daily statistics must split the time correctly.

Example result:
Yesterday → 1 hour
Today → 2 hours

XP and daily focus totals should be distributed according to the time belonging to each day.

Session history should still show the full session duration.

---

3. Add motivational messages during the focus session.

Inside the existing focus timer box, display a small text line that updates automatically while the timer runs.

Update the message when time milestones are reached:

5 minutes → "Good start."
15 minutes → "Stay focused."
30 minutes → "Half an hour. Keep going."
45 minutes → "Momentum building."
60 minutes → "Deep work achieved."
90 minutes → "Elite focus."

Requirements:

* Do not add popups.
* Do not change layout or design.
* The message should simply update inside the timer area.

---

4. Improve fullscreen focus mode.

When the user activates focus fullscreen mode:

* Use the browser Fullscreen API so the app enters true fullscreen.

Add a visible exit control in fullscreen mode:

* A small "Exit Focus Mode" button or "X" in the corner.

Clicking it should exit fullscreen.

Pressing ESC should also exit fullscreen.

---

5. Improve the focus timer animation.

Currently the timer shows a loading animation at the top of the timer box.

Replace this with an animation that moves around the border of the focus timer box while the timer is active.

Requirements:

* The animation should run only while the timer is running.
* The animation should visually move around the border of the timer container.
* The animation should stop when the timer is paused or finished.
* Do not change the layout, size, or structure of the timer component.

---

6. Improve the Focus Trend chart.

Current issue:
The focus trend chart shows activity but the Y-axis does not clearly represent what the values mean.

Required improvements:

* The chart should represent total focus time per day.
* The Y-axis should clearly show time units such as minutes or hours.
* Add a visible scale to the chart so users can understand how much focus time occurred.

Example scale:
0
30m
1h
2h
3h

Requirements:

* Do not redesign the chart layout.
* Only improve how focus time data is represented and labeled.

---

Ensure all improvements integrate correctly with the existing focus timer, XP rewards, statistics system, and charts without breaking current functionality.
