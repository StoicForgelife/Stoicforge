You are modifying the StoicForge productivity web application.

Important constraints:

* Do NOT change the existing UI layout or visual design.
* Do NOT remove or rewrite working systems.
* Only improve logic and behavior.
* The app is frontend-only and uses localStorage for persistence.

Implement the following improvements.

---

1. Fix the focus timer so it continues when the user changes tabs.

Current issue:
The timer stops when the StoicForge tab is inactive.

Required behavior:

* When the user presses Start, store the focus start timestamp.
* The timer display must calculate elapsed time using the timestamp instead of relying purely on setInterval.
* The timer must continue counting correctly even if:

  * the user switches browser tabs
  * the browser is minimized
  * the page reloads
  * the browser is closed and reopened

If the page loads and a focus session was running, automatically restore the session.

Do not change the XP reward system.

---

2. Handle focus sessions that cross midnight.

Example:
Start 11:00 PM
Finish 2:00 AM

Required behavior:

* The session remains a single continuous focus session.
* However daily statistics must split the time across days.

Example result:

Yesterday → 1 hour
Today → 2 hours

XP and daily focus totals should be distributed according to each day's portion.

Session history should still show the full session duration.

---

3. Add motivational messages while the timer is active.

Inside the existing focus timer box display a small message that updates automatically when time milestones are reached.

Example milestones:

5 minutes → "Good start."
15 minutes → "Stay focused."
30 minutes → "Half an hour. Keep going."
45 minutes → "Momentum building."
60 minutes → "Deep work achieved."
90 minutes → "Elite focus."

Requirements:

* Do not use popups.
* Do not change layout.
* The message should simply update inside the timer area.

---

4. Improve fullscreen focus mode.

Current issue:
Fullscreen mode does not use real fullscreen and has no exit control.

Required improvements:

* Use the browser Fullscreen API so the app enters true fullscreen.
* Add a visible exit control (X or "Exit Focus Mode") in the corner.
* Clicking it exits fullscreen.
* ESC should also exit fullscreen.

---

5. Improve the focus timer animation.

Current behavior:
A loading line animation appears only at the top of the focus timer box.

Required behavior:

* Replace the top loading line with a border animation that moves around the entire focus timer box.
* The animation should run while the timer is active.
* The animation should stop when the timer is paused or finished.
* Do not change layout or structure of the timer component.

---

6. Improve the Focus Trend chart.

Current issue:
The chart shows values but the Y-axis does not indicate what the values represent.

Required improvements:

* The chart should represent total focus time per day.
* Add a visible scale showing minutes or hours.

Example scale:

0
30m
1h
2h
3h

Do not redesign the chart layout. Only improve labeling and data representation.

---

7. Fix the NoFap Clean Day exploit.

Current issue:
The Clean Day button can be clicked multiple times in the same day which falsely increases the streak.

Required behavior:

* A Clean Day can only be logged once per calendar day.
* Store the last clean day date in localStorage.
* When the button is clicked:

  * If the user already logged a clean day today, do not increase the streak and show a small message.
  * If it is a new day, increase the streak and update the stored date.

Do not change the UI design of the NoFap section.

---

Ensure all improvements integrate correctly with the existing systems including XP rewards, statistics tracking, charts, and streak systems without breaking current functionality.
